编写管理员界面，在客户端登陆时判定，如为管理员则显示管理员界面。

修图时判断文件是否需要返工，若需要则将需返工文件名提交至服务器，服务器端将返工任务储存在数据库中
并在待返工员工下次登陆时提示有返工任务。

修图完成后管理员在软件内审核，如需返工同上一步处理。

管理员有权管理新注册的用户，用户注册完毕后存储在unreviewed_user表中，管理员将需要通过的员工通过后，
员工自动转移至user表中。

管理员重置员工密码。